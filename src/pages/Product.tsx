import AdminSidebar from "../component/AdminSidebar";

const Product = () => {
	return (
		<div className='admin-container'>
			<AdminSidebar />
			<main>dashboard</main>
		</div>
	);
};

export default Product;
